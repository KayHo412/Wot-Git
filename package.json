{
  "name": "wot-git",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter @wot-git/api --filter @wot-git/web dev",
    "dev:api": "pnpm --filter @wot-git/api dev",
    "dev:web": "pnpm --filter @wot-git/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "pnpm": "^11.27.1"
  }
}
